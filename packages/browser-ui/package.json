{
  "name": "@agent-secrets/browser-ui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run build:vendor",
    "build:vendor": "esbuild src/hpke-entry.js --bundle --format=esm --platform=browser --target=es2022 --outfile=vendor/hpke.js"
  },
  "dependencies": {
    "hpke-js": "^1.7.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
