{
    "id": "agent-kryptos",
    "name": "Secure Secret Provisioning",
    "version": "0.1.0",
    "description": "Zero-knowledge secret provisioning via HPKE encryption. Registers a request_secret tool for the agent.",
    "skills": [
        "skills/agent-kryptos"
    ],
    "configSchema": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
            "SPS_BASE_URL": {
                "type": "string",
                "description": "Base URL of the Secret Provisioning Service (e.g., http://localhost:3100)",
                "default": "http://localhost:3100"
            },
            "AGENT_KRYPTOS_AGENT_ID": {
                "type": "string",
                "description": "Stable unique identity for this agent in the Kryptos system",
                "default": "agent-kryptos-agent"
            },
            "OPENCLAW_AGENT_TARGETS_JSON": {
                "type": "string",
                "description": "Optional mapping of agent IDs to session targets for fulfillment delivery"
            }
        }
    }
}